function getInfoFromNative() {//原生调用
	alert(1)
	// let obj = JSON.parse(returnStr);
	// if (obj.code == -3) {
	// 	Vue.isTxtShow = true;
	// 	Vue.msg = '您好，系统出错';
	// 	setTimeout(function() {
	// 		Vue.isTxtShow = false;
	// 		Vue.$router.replace({path:'/member/order/orderDet/'+data.order_id});
	// 	},1500);
	// 	return;
	// }
	// Vue.$router.replace({path:'/member/order/orderDet/'+data.order_id});
}
export default  getInfoFromNative;